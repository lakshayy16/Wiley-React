const Welcome = ({name,desig}) => 
{
    // console.log(props)
    return(<h1>Welcome {name} on your first course on react! 
                    Your role would be as a {desig}</h1>)
}

export default Welcome;